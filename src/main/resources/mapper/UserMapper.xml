<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.competition.dao.UserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.competition.entity.User">
        <id column="user_id" property="userId" />
        <result column="password" property="password" />
        <result column="portrait" property="portrait" />
        <result column="introduction" property="introduction" />
        <result column="user_name" property="userName" />
        <result column="realname" property="realname" />
        <result column="user_no" property="userNo" />
        <result column="user_school" property="userSchool" />
        <result column="user_major" property="userMajor" />
        <result column="user_sex" property="userSex" />
        <result column="user_birth" property="userBirth" />
        <result column="user_phone" property="userPhone" />
        <result column="user_email" property="userEmail" />
        <result column="file" property="file" />
        <result column="hash" property="hash" />
        <result column="is_active" property="isActive" />
        <result column="ip_address" property="ipAddress" />
        <result column="check" property="check" />
        <result column="createAt" property="createAt" />
        <result column="updateAt" property="updateAt" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        user_id, password, portrait, introduction, user_name, realname, user_no, user_school, user_major, user_sex, user_birth, user_phone, user_email, file, hash, is_active, ip_address, 'check', createAt, updateAt
    </sql>

</mapper>
