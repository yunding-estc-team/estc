<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.competition.dao.CompetitionCheckoutMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.competition.entity.CompetitionCheckout">
        <result column="competitionId" property="competitionId" />
        <result column="id" property="id" />
        <result column="file" property="file" />
        <result column="hash" property="hash" />
        <result column="user_id" property="userId"/>
        <result column="checkout" property="checkout"/>
        <result column="description" property="description"/>
        <result column="create_at" property="createAt"/>
        <result column="update_at" property="updateAt"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id,competitionId, file, hash,userId,checkout,description,createAt,updateAt
    </sql>

</mapper>
